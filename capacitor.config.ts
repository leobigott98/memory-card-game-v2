import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.doritos.memorygame',
  appName: 'Doritos Memory Game',
  webDir: 'dist'
};

export default config;
